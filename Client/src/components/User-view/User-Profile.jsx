import React, { useState, useEffect } from "react";

import { SheetContent } from "../ui/sheet";

const UserProfileSheetContent = ({ userId }) => {
  return (
    <SheetContent>
      <div></div>
    </SheetContent>
  );
};

export default UserProfileSheetContent;
